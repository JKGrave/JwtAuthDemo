server:
  port: 8083

spring:
  profiles:
    active: localdev
  application:
    name: client

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:8761/eureka