server:
  port: 8082
spring:
  profiles:
    active: localdev
  application:
    name: user

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:8761/eureka